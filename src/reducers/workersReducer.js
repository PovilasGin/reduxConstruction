const initialState=[
    {name:'bill', status:'admin', description:'hard worker'},
    {name:'annabele', status:'cleaner', description:'good worker'}    
]

const workers = (state=initialState, action)=>{
    return state
}

export default workers